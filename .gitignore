# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env.local
.env*.local

# typescript
*.tsbuildinfo

# data: keep the input CSV and geocache out of git (large / may contain owner PII).
# The generated public/hotels.geojson IS committed so Vercel can render without a build step.
/data/hotels.csv
/data/geocache.json
